<VirtualHost *:8080>
  ServerName nexv-el.com
  DocumentRoot /home/site/web/nexv-el.com/public_html/prod

  <Directory “/home/site/web/nexv-el.com/public_html/prod">

    RewriteEngine On
    # Don't rewrite files or directories
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    # Rewrite everything else to index.html to allow html5 state links
    RewriteRule ^ index.html [L]
  </Directory>
</VirtualHost>
